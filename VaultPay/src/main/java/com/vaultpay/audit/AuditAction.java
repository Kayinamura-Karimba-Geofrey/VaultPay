package com.vaultpay.audit;

public enum AuditAction {
    USER_REGISTERED,
    USER_LOGGED_IN,
    USER_LOGGED_OUT,
    WALLET_BALANCE_VIEWED,
    MONEY_TRANSFERRED,
    ADMIN_LOCKED_USER,
    ADMIN_UNLOCKED_USER
}
